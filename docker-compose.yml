version: '3.8'
services:
  app:
    build: ./ap_dashboard
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=production
    volumes:
      - ./ap_dashboard:/app
    restart: unless-stopped
